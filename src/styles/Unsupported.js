import styled from 'styled-components'

export const UnsupportedStyle = styled.span`
    .unsupported {
        padding: 46px;
        background: white;
        margin: auto;
        text-align: center;
    }
`
